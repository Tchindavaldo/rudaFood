import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Item3CmdBottomCardPage } from './item3-cmd-bottom-card.page';

describe('Item3CmdBottomCardPage', () => {
  let component: Item3CmdBottomCardPage;
  let fixture: ComponentFixture<Item3CmdBottomCardPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Item3CmdBottomCardPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
