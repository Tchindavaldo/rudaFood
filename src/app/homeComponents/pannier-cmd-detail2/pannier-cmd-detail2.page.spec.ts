import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PannierCmdDetail2Page } from './pannier-cmd-detail2.page';

describe('PannierCmdDetail2Page', () => {
  let component: PannierCmdDetail2Page;
  let fixture: ComponentFixture<PannierCmdDetail2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PannierCmdDetail2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
